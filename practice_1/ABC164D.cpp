#include <bits/stdc++.h>          
using namespace std;               
typedef long long ll;

int main() {
    string str;
    cin >> str;

    vector<int> S(str.size());
    for (ll i=0; i<str.size(); i++) {
        S.at(i) = str.at(i)-'0';
    }

    //
    int i,j;
    int N = 200000/2019;
    vector<int> year
}