#include <bits/stdc++.h>          
using namespace std;      
typedef long long ll;         

int main() {
    int S;
    cin >> S;
    ll mod = pow(10,9)+7;
    ll maxN = S/3;
    if (maxN == 0) {
        cout << 0 << endl;
        exit(0);
    }
    if (S%3 == 0) {
        
    }
}